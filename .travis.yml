dist: bionic
services:
  - docker
language: python
python:
  - "3.6"
  - "3.7"
  - "3.8"
before_install:
  - bash ./test.sh
install:
  - pip install -r requirements.txt
script:
  - python -m pytest --cov=pysirix -v
